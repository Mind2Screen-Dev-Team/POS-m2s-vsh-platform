# IOS-KAT-002 — Task Contract: SwiftUI Detail Produk

schema_version: "1.0"

task:
  id: IOS-KAT-002
  title: "Halaman detail produk di SwiftUI iOS"
  type: frontend-implementation
  project: pos-katalog
  status: technical-ready
  requirement_ids:
    - REQ-POS-KAT-007
  business_rule_ids:
    - BL-IOS-KAT-001
  contract_ids:
    - CONTRACT-KAT-001

ownership:
  role: ios-developer
  repository: POS-ios
  base_branch: develop
  branch: agent/IOS-KAT-002-swiftui-detail-produk

execution:
  isolation: worktree
  background: true
  max_turns: 80
  timeout_minutes: 60

paths:
  allowed:
    - apps/ios/**
    - ios/**
    - Package.swift
  forbidden:
    - .claude/**
    - .task/**

inputs:
  - contracts/CONTRACT-KAT-001.yaml

acceptance_criteria:
  - "Detail menampilkan foto, nama, deskripsi, harga beli, harga jual, stok, kategori"
  - "Tombol Edit membuka form"
  - "Tombol Hapus menampilkan konfirmasi"
  - "Navigasi kembali ke daftar"
  - "Unit test: renders detail, navigation"

quality_gates:
  - swift build
  - swift test

outputs:
  - code
  - unit-tests
  - pull-request

stop_conditions:
  - contract change required
  - dependency required